<UserControl x:Class="Application.HailuBoard.PartsInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodel="clr-namespace:Application.HailuBoard"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="https://handyorg.github.io/handycontrol"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance viewmodel:PartsInfoViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding RefreshCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock 
                Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="20"
                FontWeight="Bold"
                Text="数据总览" 
                Margin="5"/>
            <StackPanel Grid.Row="1"
                        Margin="5"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                <TextBlock
                    Margin="5"
                    VerticalAlignment="Center"
                    Text="设定当前批次：" />
                <TextBox Margin="5"
                         Width="250"/>
                <Button
                    Width="150"
                    Margin="5"
                    Background="#47a0ff"
                    Content="设定批次"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Command="{Binding LoginCommand}"/>
            </StackPanel>
        </Grid>
        <DataGrid 
           Grid.Row="1"
           Margin="0,5,0,10"
           controls:DataGridAttach.CanUnselectAllWithBlankArea="True"
           AutoGenerateColumns="False"
           CanUserAddRows="False"
           HeadersVisibility="All"
           IsReadOnly="True"
           ItemsSource="{Binding Items}"
           MinRowHeight="45"
           RowHeaderWidth="60"
           SelectionMode="Single"
           SelectionUnit="FullRow">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <CheckBox Margin="20,0,0,0" IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Id" 
                   Header="序号" 
                   Binding="{Binding Id}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Quautity" 
                   Header="数量总计" 
                   Binding="{Binding Quautity}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Countinfo" 
                   Header="当前已处理板件数量" 
                   Binding="{Binding Countinfo}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="BatchCode" 
                   Header="批次号" 
                   Binding="{Binding BatchCode}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Code" 
                   Header="编码" 
                   Binding="{Binding Code}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Name" 
                   Header="名称" 
                   Binding="{Binding Name}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="CodeName" 
                   Header="代号" 
                   Binding="{Binding CodeName}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Length" 
                   Header="长度" 
                   Binding="{Binding Length}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Width1" 
                   Header="宽度" 
                   Binding="{Binding Width1}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Thickness" 
                   Header="厚度" 
                   Binding="{Binding Thickness}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Remark" 
                   Header="铣刀数据" 
                   Binding="{Binding Remark}"/>
                <DataGridTextColumn 
                   Width="Auto" 
                   CanUserSort="True" 
                   SortMemberPath="Area" 
                   Header="平方数" 
                   Binding="{Binding Area}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2"
            Margin="0,5,0,5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <materialDesign:PackIcon
                Width="24"
                Height="24"
                Cursor="Hand"
                Kind="ChevronLeft">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction Command="{Binding PreviousPageCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </materialDesign:PackIcon>
            <TextBlock
                Margin="10,0"
                VerticalAlignment="Center"
                Text="{Binding pageNumber, StringFormat='第{0}页'}" />
            <materialDesign:PackIcon
                Width="24"
                Height="24"
                Cursor="Hand"
                Kind="ChevronRight">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction Command="{Binding NextPageCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </materialDesign:PackIcon>
            <TextBlock
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                Text="{Binding totalPage, StringFormat='共{0}页'}" />
        </StackPanel>
    </Grid>
</UserControl>
