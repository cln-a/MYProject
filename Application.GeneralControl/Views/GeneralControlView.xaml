<UserControl x:Class="Application.GeneralControl.GeneralControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodel="clr-namespace:Application.GeneralControl"
             xmlns:behavior = "http://schemas.microsoft.com/xaml/behaviors"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="1080" 
             d:DesignWidth="1920"
             d:DataContext="{d:DesignInstance viewmodel:GeneralControlViewModel}"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="https://handyorg.github.io/handycontrol"
             xmlns:conv="clr-namespace:Application.UI;assembly=Application.UI"
             FontFamily="Microsoft YaHei"
             TextElement.FontSize="16"
             TextElement.FontWeight="Regular"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <UserControl.Resources>
        <conv:BoolToTextConverter x:Key="BoolToTextConverter" />
        <conv:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Margin="5"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                <TextBlock
                    Grid.Row="0"
                    Text="{DynamicResource 设定耗时时间(单位:s)}" 
                    HorizontalAlignment="Center" 
                    Style="{StaticResource TextBlockBaseStyle}" 
                    Margin="5"/>
                <Rectangle Width="10"/>
                <TextBox
                    x:Name="SourceTextbox"
                    Margin="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="100"
                    conv:TextBoxInputBehavior.IsDecimalOnly="True"
                    conv:TextBoxInputBehavior.DecimalPlaces="2"
                    Text="{Binding GeneralControlModel.SetTime}"/>
                <Button
                    MinWidth="200"
                    Margin="10"
                    Background="#4B91E2"
                    Content="{DynamicResource 设定耗时时间}"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Command="{Binding SetTimeCommand}"/>
                <TextBlock
                    Grid.Row="0"
                    Text="{DynamicResource 设定延时时间(单位:s)}" 
                    HorizontalAlignment="Center" 
                    Style="{StaticResource TextBlockBaseStyle}" 
                    Margin="5"/>
                <TextBox
                    x:Name="DelayTextbox"
                    Margin="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="100"
                    conv:TextBoxInputBehavior.IsDecimalOnly="True"
                    conv:TextBoxInputBehavior.DecimalPlaces="2"
                    Text="{Binding GeneralControlModel.SetDelayTime}"/>
                <Button
                    MinWidth="200"
                    Margin="10"
                    Background="#4B91E2"
                    Content="{DynamicResource 设定延时时间}"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Command="{Binding SetDelayTimeCommand}"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Margin="5"
                        BorderThickness="0.5"
                        BorderBrush="#3F6A7F"
                        CornerRadius="10">
                    <ScrollViewer 
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Style="{StaticResource ScrollViewerUpDown}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Text="{DynamicResource 设定产线参数}" 
                                HorizontalAlignment="Center" 
                                Style="{StaticResource TextBlockBaseStyle}" 
                                FontSize="25"
                                FontWeight="Bold"
                                Margin="5"/>
                            <UniformGrid
                                    Grid.Row="2"
                                    Rows="11"
                                    Columns="1">
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位1}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableOne, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableOne, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableOneCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}" />
                                    <TextBox Margin="0"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位2}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableTwo, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableTwo, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableTwoCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位3}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableThree, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableThree, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableThreeCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位4}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"  
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableFour, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableFour, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableFourCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位5}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableFive, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableFive, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableFiveCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位6}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableSix, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableSix, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableSixCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位7}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}"/>
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableSeven, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableSeven, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableSevenCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"    
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}"/>
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位8}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"       
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableEight, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableEight, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableEightCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位9}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableNine, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableNine, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableNineCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"
                                        IsReadOnly="True"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位10}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableTen, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableTen, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableTenCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 耗时时间(单位:s)：}"  />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"
                                        IsReadOnly="True"/>
                                </StackPanel>
                                <StackPanel Margin="5"
                                        HorizontalAlignment="Center"
                                        Orientation="Horizontal">
                                    <TextBlock Margin="5"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 工位11}" />
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 是否启用：}" />
                                    <Button
                                        Width="120"
                                        Margin="10"
                                        Background="{Binding SetParamsFactory.SetEnableEleven, Converter={StaticResource BoolToBrushConverter}}"
                                        Content="{Binding SetParamsFactory.SetEnableEleven, Converter={StaticResource BoolToTextConverter}}"
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        Command="{Binding EnableElevenCommand}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                       Text="{DynamicResource 耗时时间(单位:s)：}" />
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=SourceTextbox,Path=Text,Mode=OneWay}"/>
                                    <TextBlock Margin="10"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="#1b2755"
                                        Style="{StaticResource TextBlockDefaultSecLight}"
                                        Text="{DynamicResource 延时时间(单位:s)：}"/>
                                    <TextBox Margin="10"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="120"
                                        IsReadOnly="True"
                                        Text="{Binding ElementName=DelayTextbox,Path=Text,Mode=OneWay}"/>
                                </StackPanel>
                            </UniformGrid>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
