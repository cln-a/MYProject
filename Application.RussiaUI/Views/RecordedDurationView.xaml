<UserControl
    x:Class="Application.RussiaUI.RecordedDurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="https://handyorg.github.io/handycontrol"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodel="clr-namespace:Application.RussiaUI"
    d:DataContext="{d:DesignInstance viewmodel:RecordedDurationViewModel}"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="White"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="30"
            FontWeight="Bold"
            Text="{DynamicResource 工位计时页面}" />
        <DataGrid
            x:Name="ProduceParameterDataGrid"
            Grid.Row="1"
            Margin="0,5,0,10"
            controls:DataGridAttach.CanUnselectAllWithBlankArea="True"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            HeadersVisibility="All"
            IsReadOnly="True"
            ItemsSource="{Binding WorkStationManager.WorkStations}"
            MinRowHeight="45"
            RowHeaderWidth="60"
            SelectionMode="Extended"
            SelectionUnit="FullRow">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <CheckBox Margin="20,0,0,0" IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Id}"
                    Header="序号" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding SetTime}"
                    Header="工位设定耗时时间" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding SetDelayTime}"
                    Header="工位设定延时时间" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding PastTime}"
                    Header="当前工位耗时时间（开始）" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding RecordedTime}"
                    Header="当前工位耗时时间（结束）" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
